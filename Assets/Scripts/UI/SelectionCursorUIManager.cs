using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SelectionCursorUIManager : MonoBehaviour
{

    public List<SelectionCursorUI> selectionCursors = new List<SelectionCursorUI>();

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
